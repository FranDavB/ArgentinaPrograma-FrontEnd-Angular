export interface Experiences{
    id?: number;
    name: string;
    logourl: string;
    description: string;
    date: string;
}

export interface Credentials{
    email: string;
    password: string;
}